# I 阶段本机 GPU 持续授权记录

状态：生效。授权时间：2026-08-14T14:52:13+08:00。

## 授权证据

用户在当前项目聊天中明确说明：仅针对中尺度实验计划，不需要再寻求批准，可以直接使用 GPU 进行实验训练；每完成一个任务检查是否偏离任务目标，避免因上下文或任务时间过长产生错误。

## 适用范围

- 仅适用于 `11_RTX5060中尺度实验计划.md` 中 `I-GATE-A` 至 `I-09` 的本机 RTX 5060 动作。
- 允许按冻结计划依次执行生成、攻击、mixed、数据组装、特征提取、tensor bundle、10 个训练、Dev 分析、Calibration、一次性 Test、报告和 QA。
- 每个运行时动作仍须单独冻结 scope、配置哈希、输入哈希、输出位置和审批指纹，并生成独立限时审批记录；这些记录引用本持续授权，不再逐次等待新的聊天批准。
- 每个任务完成后先执行目标偏离审计；只有未偏离冻结目标且验收通过，才进入下一任务。

## 不适用范围

- 不授权 H100 正式实验、其他项目、下载新模型、修改旧 `project1` 或超出冻结规模的运行。
- 不授权根据运行结果静默缩减样本、改变统计协议、解封顺序、训练矩阵或停止条件。
- 若 canary 推算总时长超过 24 小时、失败率超过 1%、发生不可恢复 OOM，或触发温度、内存、磁盘、CUDA、非有限值、哈希漂移停止条件，必须停止并保留 partial；持续授权不覆盖绕过停止条件。
